const express = require("express");
const router = express.Router();
const ListingController = require('../controllers/listing.controller');

// Create routes for categorg here

module.exports = router;
