const express = require("express");
const router = express.Router();
const ListingController = require('../controllers/listing.controller');

// Create routes for listing here

module.exports = router;
